example usage: php example.php

For those who don't have PHP installed:

docker build -t sorted-linked-list . && docker run --rm sorted-linked-list // This will run example.php in the root directory.
